# Header details for the first \cornellpage in cornell-notes.tex.
# Run `python3 scripts/yaml_to_header.py` to regenerate build/cornell-header.tex, then
# compile cornell-notes.tex with pdflatex as usual.
topic: Weekly Sync
date: 2026-08-23
attendees: Alice, Bob, Priya
time: "10:00--10:30, Zoom"
project: "Project Atlas"
